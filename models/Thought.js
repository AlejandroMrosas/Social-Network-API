const { Schema, model } = require('mongoose');

const reaction = new Schema({
    reactionId: { 
        type: String
    },
    reactionBody: {

    },
    username: {

    },
    createdAt: {
        
    }
})